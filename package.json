{
  "name": "nuxt-web3-auth",
  "version": "1.0.0",
  "description": "A demo which uses nuxt and web3/metamask to sign in users and protect routes.",
  "author": "Tobias Thiele",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others \"nuxt\" \"cd api && npm run dev\"",
    "build": "nuxt build",
    "start": "nuxt build && concurrently --kill-others \"nuxt start\" \"cd api && npm run start\"",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.3.6",
    "bootstrap": "^4.3.1",
    "js-cookie": "^2.2.0",
    "nuxt": "^2.4.0",
    "web3": "^1.0.0-beta.48"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "node-sass": "^4.11.0",
    "nodemon": "^1.18.9",
    "sass-loader": "^7.1.0"
  }
}
